<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    package="dev.eder.androidcourse">

    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_FINE_LOCATION" />
    <uses-permission android:name="android.permission.ACCESS_LOCATION_EXTRA_COMMANDS" />
    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:name=".AndroidCourseApplicattion"
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme"
        tools:ignore="GoogleAppIndexingWarning">
        <activity
            android:name=".lifecycle.MainActivity"
            android:screenOrientation="portrait" />
        <activity
            android:name=".profilesample.SecondActivity"
            android:screenOrientation="portrait" />
        <activity
            android:name=".components.ComponentsActivity"
            android:screenOrientation="portrait" />
        <activity
            android:name=".components.MoreComponentsActivity"
            android:screenOrientation="portrait" />
        <activity
            android:name=".menu.MenuSampleActivity"
            android:screenOrientation="portrait" />
        <activity
            android:name=".menu.SubMenuSampleActivity"
            android:screenOrientation="portrait" />
        <activity
            android:name=".notificationsample.FirstActivity"
            android:screenOrientation="portrait" />
        <activity
            android:name=".notificationsample.SecondActivity"
            android:screenOrientation="portrait" />
        <activity
            android:name=".framesample.FrameSampleActivity"
            android:screenOrientation="portrait" />
        <activity android:name=".location.LocationActivity" />
        <activity android:name=".profilesample.ProfileActivity" />
        <activity android:name=".goodviewpager.GoodViewPagerActivity" />
        <activity android:name=".db.RealmSampleActivity" />
        <activity
            android:name=".db.EditShopActivity"
            android:screenOrientation="portrait" />
        <activity
            android:name=".draw.CanvasAndPaintActivity"
            android:screenOrientation="portrait" />
        <activity
            android:name=".musicplayer.MusicActivity"
            android:screenOrientation="portrait" />
        <activity android:name=".videoplayer.VideoActivity"></activity>
        <activity android:name=".animations.AnimationsSamplesActivity">

        </activity>
        <!--
             The API key for Google Maps-based APIs is defined as a string resource.
             (See the file "res/values/google_maps_api.xml").
             Note that the API key is linked to the encryption key used to sign the APK.
             You need a different API key for each encryption key, including the release key that is used to
             sign the APK for publishing.
             You can define the keys for the debug and release targets in src/debug/ and src/release/. 
        -->
        <meta-data
            android:name="com.google.android.geo.API_KEY"
            android:value="@string/google_maps_key" />

        <activity
            android:name=".location.MapsSampleActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>